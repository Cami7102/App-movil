<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myfirstapplication">

    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Lost All Hope"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFirstApplication">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".NotaUno"/>
        <activity android:name=".inicio.Test"/>
        <activity android:name=".TestDos"/>
        <activity android:name=".NotaDos"/>
        <activity android:name=".PantallaPrincipalMenu"/>
        <activity android:name=".calendario.Calendario"/>
        <activity android:name=".configuraciones.Configuraciones"/>
        <activity android:name=".configuraciones.Color"/>
        <activity android:name=".configuracion.configuraciones.CambiarContrasenia"/>
        <activity android:name=".configuracion.configuraciones.EliminarUsuario"/>
        <activity android:name=".configuracion.configuraciones.Fondo"/>
        <activity android:name=".configuracion.configuraciones.Test"/>

    </application>

</manifest>